int
main() {
#if 1
    int x = 10;
    if (x == 10) {
        return 20 / x;
    } else {
        return x;
    }
    return y; // expects: 2
    
#elif 0
    int x = 10;
    int y = 0;
    for (int i = 0; i < x; i += 1) {
        y += 2;
    }
    
    return y; // expects: 20
    
#elif 0
    // Truncating integers
    s64 x = 140234230023;
    return (int) x; // expects: -1499690745
    
#elif 0
    // Sign extends and wrapping integers
    s8 x = (s8) -10;
    x += (s8) 200;
    
    int y = (int) x;
    return y; // expects: -66
#endif
}