
struct Type_Context {
    
};



void
check_procedure_signature(Type_Context* tcx, Ast_Proc_Type* sig) {
    
    // infer types used in signature (return + args) make sure they are valid types
    
    sig->return_type;
    sig->args;
}
